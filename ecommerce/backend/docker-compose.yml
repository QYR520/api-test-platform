# 电商后端一键部署：docker compose up -d --build
services:
  ecommerce-backend:
    build: .
    container_name: ecommerce-backend
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data   # 用户/订单数据持久化，容器重启不丢失
    restart: unless-stopped
